<%- include('../includes/head.ejs') %>

<body>
    <%- include('../includes/navigation.ejs') %>

    <h1>👨Friend Requests 👨</h1>


    <h3>Testing</h3>

    

    <% if( friendRequests.length > 0) { %>
        <div class="container">
            <table class="w3-table">
                <tr>
                    <th>UserName</th>
                    <th>Email</th>
                    <th>Action</th>
                </tr>
                <% for(let request of friendRequests)  { %>
                <tr>
                    <td><%= request.username %></td>
                    <td><%= request.email %></td>
                    <td>
                        <form action="/accept-request/<%= request.id %>" method="post">
                            <button class="btn" type="submit">Accept Request</button>
                        </form>
                        <form action="/decline-request/<%= request.id %>" method="post">
                            <button class="btn" type="submit" >Decline Request</button>
                        </form>
                        <button class="toggle" onclick="ne()"></button>

                        <!-- enctype must be set to the type you will parse before methodOverride() -->
                <!-- <form method="POST" action="/decline-request/<%= request.id %>" enctype="application/x-www-form-urlencoded">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit">Delete resource</button>
                </form>
   -->
                    </td>
                </tr>
                <% } %>
                
                <% }  else {%>
                    <h1>No Friend Request Yet!</h1>
                <% } %>
    
            </table>

            <script src="main.js"></script>
            <!-- <script type="text/javascript" src="/public/js/main.js"></script> -->
</body>
</html>